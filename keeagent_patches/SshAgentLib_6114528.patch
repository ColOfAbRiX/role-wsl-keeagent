diff --git a/SshAgentLib/CygwinSocket.cs b/SshAgentLib/CygwinSocket.cs
index 7110e43..d8ecdfe 100644
--- a/SshAgentLib/CygwinSocket.cs
+++ b/SshAgentLib/CygwinSocket.cs
@@ -64,7 +64,8 @@ public class CygwinSocket : IDisposable
         /// Create new "unix domain" socket for use with Cygwin
         /// </summary>
         /// <param name="path">The name of the file to use for the socket</param>
-        public CygwinSocket(string path)
+        /// <param name="bindAddress">The IP address to bind to. Defaults to loopback if null.</param>
+        public CygwinSocket(string path, IPAddress bindAddress = null)
         {
             this.path = path;
             guid = Guid.NewGuid();
@@ -91,7 +92,7 @@ public CygwinSocket(string path)
                         SocketType.Stream,
                         ProtocolType.Tcp
                     );
-                    var endpoint = new IPEndPoint(IPAddress.Loopback, 0);
+                    var endpoint = new IPEndPoint(bindAddress ?? IPAddress.Loopback, 0);
                     socket.Bind(endpoint);
                     socket.Listen(5);
                     var socketThread = new Thread(AcceptConnections);
diff --git a/SshAgentLib/MsysSocket .cs b/SshAgentLib/MsysSocket .cs
index e9808bc..5004e13 100644
--- a/SshAgentLib/MsysSocket .cs	
+++ b/SshAgentLib/MsysSocket .cs	
@@ -66,7 +66,8 @@ public class MsysSocket : IDisposable
         /// Create new "unix domain" socket for use with MSYS
         /// </summary>
         /// <param name="path">The name of the file to use for the socket</param>
-        public MsysSocket(string path)
+        /// <param name="bindAddress">The IP address to bind to. Defaults to loopback if null.</param>
+        public MsysSocket(string path, IPAddress bindAddress = null)
         {
             this.path = path;
             guid = Guid.NewGuid();
@@ -93,7 +94,7 @@ public MsysSocket(string path)
                         SocketType.Stream,
                         ProtocolType.Tcp
                     );
-                    var endpoint = new IPEndPoint(IPAddress.Loopback, 0);
+                    var endpoint = new IPEndPoint(bindAddress ?? IPAddress.Loopback, 0);
                     socket.Bind(endpoint);
                     socket.Listen(5);
                     var socketThread = new Thread(AcceptConnections);
diff --git a/SshAgentLib/PageantAgent.cs b/SshAgentLib/PageantAgent.cs
index 73773d6..a1f22f7 100644
--- a/SshAgentLib/PageantAgent.cs
+++ b/SshAgentLib/PageantAgent.cs
@@ -6,6 +6,7 @@
 using System.IO;
 using System.IO.MemoryMappedFiles;
 using System.IO.Pipes;
+using System.Net;
 using System.Net.Sockets;
 using System.Runtime.InteropServices;
 using System.Security.Principal;
@@ -238,7 +239,8 @@ public static bool CheckPageantRunning()
         /// that comes with cygwin.
         /// </summary>
         /// <param name="path">The path to the socket file that will be created.</param>
-        public void StartCygwinSocket(string path)
+        /// <param name="bindAddress">The IP address to bind to. Defaults to loopback if null.</param>
+        public void StartCygwinSocket(string path, IPAddress bindAddress = null)
         {
             if (disposed)
             {
@@ -258,7 +260,7 @@ public void StartCygwinSocket(string path)
                 File.Delete(path);
             }
 
-            cygwinSocket = new CygwinSocket(path) { ConnectionHandler = ConnectionHandler };
+            cygwinSocket = new CygwinSocket(path, bindAddress) { ConnectionHandler = ConnectionHandler };
         }
 
         public void StopCygwinSocket()
@@ -282,7 +284,8 @@ public void StopCygwinSocket()
         /// that comes with msysgit.
         /// </summary>
         /// <param name="path">The path to the socket file that will be created.</param>
-        public void StartMsysSocket(string path)
+        /// <param name="bindAddress">The IP address to bind to. Defaults to loopback if null.</param>
+        public void StartMsysSocket(string path, IPAddress bindAddress = null)
         {
             if (disposed)
             {
@@ -302,7 +305,7 @@ public void StartMsysSocket(string path)
                 File.Delete(path);
             }
 
-            msysSocket = new MsysSocket(path) { ConnectionHandler = ConnectionHandler };
+            msysSocket = new MsysSocket(path, bindAddress) { ConnectionHandler = ConnectionHandler };
         }
 
         public void StopMsysSocket()
